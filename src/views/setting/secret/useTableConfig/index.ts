export const useTableConfig = () => {
    const scrollbar = false
    const scroll = { x: 'max-content' }
    const bordered = false

    return {
        scrollbar,
        scroll,
        bordered
    }
}